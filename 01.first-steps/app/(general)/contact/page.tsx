import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact",
  description: "Contact Page",
  
};

export default function ContactPage() {
  return (
    <div>
      <span className="text-6xl text-center">ContactPage</span>
    </div>
  );
}
